# .env.example
# This application is a strictly frontend React application using Vite.
# No environment variables are strictly required to run this application offline locally.

# VITE_API_BASE_URL=http://localhost:3000   # (Example: Uncomment and set if you connect a local backend)
